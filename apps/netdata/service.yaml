apiVersion: v1
kind: Service
metadata:
  name: netdata
  namespace: bijouxlabs
  labels: { app: netdata }
spec:
  type: LoadBalancer
  selector:
    app: netdata
  ports:
    - name: http
      port: 19999
      targetPort: 19999
      protocol: TCP
